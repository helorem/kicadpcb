GRID = 1.27 # track are aligned on the grid (default value from kicad)
FLOAT_HACK = 0.000001 #hack to handle float comparaison bug
WORLD_MARGIN = GRID * 3 # The board size is created from the size of components, but we add a margin in case track need it.
TRACK_SIZE = 0.250 # width of tracks (default value from kicad)
